    // ===== NOVA VPN: handle permission =====
    private BoostCorePlugin pendingBoostPlugin;

    @Override
    protected void onActivityResult(int requestCode, int resultCode, Intent data) {
        super.onActivityResult(requestCode, resultCode, data);
        if (requestCode == 100) {
            // permission برای WireGuard VPN
            if (resultCode == RESULT_OK) {
                // Intent مجدد به service بفرست
            }
        }
    }
